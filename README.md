# RE:FEEEL AX

옵시디언 볼트(로컬) 위에 얹은 개인 워크스페이스 대시보드.

- **데스크톱 크롬/엣지 전용** — File System Access API로 로컬 볼트 폴더를 직접 읽습니다(데이터는 서버에 올라가지 않음).
- AI 기능(요약·채점)은 로컬 서버(`/summarize`·`/grade`)가 떠 있을 때만 동작합니다. (정적 호스팅에선 읽기/보기만)
- 모바일은 미지원(File System Access 부재) — 별도 빌드 예정.
